  <div class="container" style="margin-top:20px;" >
  <nav class="navbar bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path %>">
     <img src="<%= asset_url('logo.png') %>" alt="Logo" width="45" height=40"" class="d-inline-block align-text-top">
      BOOK APP
    </a>
    <%#= link_to "Create New TODO", new_todo_path, {class: "btn btn-outline-primary"} %>
  </div>
</nav>
<div class="text-success">
  <hr>
</div>
<div class="container" >
<% @author.each do |auth|%>
<div class="card text-center"style="margin-top:20px;" >
  <div class="card-header">
 <h5 class="card-title"><%= link_to auth.name.upcase, author_path(auth.id) %></h5>
  </div>
  <div class="card-body">
    <p class="card-text"><strong>Location:</strong> <%=auth.location %></p>
    <p class="card-text">Has <%= auth.books.count %> Book/s <%=link_to "View", book_path(auth.id), {class: "btn btn-outline-primary"}  %></p>
  </div>

</div>

<% end%>

</div>

  </div>